#include "map.h"

Map::Map()
{
}

Map::~Map()
{
}

void Map::Init()
{
	cin >> x >> y;
	//block = new Cube*[x];
	//for (int i = 0; i < y; i++)
	//	block[i] = new Cube[y];

	//for (int i = 0; i < x; i++) {
	//	for (int j = 0; j < y; j++) {
	//		block[x][y].position = vec3(i - x / 2, 0, j - y / 2);

	//		block[x][y].scale = vec3(1.0, 1.0, 1.0);

	//		block[x][y].Init();
	//	}
	//}

	//

}

void Map::Update()
{
}
